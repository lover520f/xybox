import 'package:flutter/material.dart';
class VodPage extends StatelessWidget { const VodPage({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('影视')), body: const Center(child: Text('VOD'))); }
